version: "3.2"
services:
  dd-opentracing-cpp-example:
    build:
      context: ./
    depends_on:
      - dd-agent
    stdin_open: true
    tty: true
  dd-agent:
    build:
      context: ./
      dockerfile: agent.dockerfile
